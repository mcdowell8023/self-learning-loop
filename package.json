{
  "name": "@openclaw/self-learning-loop",
  "version": "1.1.0-alpha.6",
  "private": true,
  "type": "module",
  "description": "Cross-runtime self-learning skill for AI agents \u2014 extract reusable rules from conversations and graduate them into AGENTS.md / TOOLS.md automatically",
  "bin": {
    "openclaw-learn": "./dist/cli/learn.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/store/migrations && cp src/store/migrations/*.sql dist/store/migrations/",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run build && npm test"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "simple-statistics": "^7.8.9",
    "yaml": "^2.5.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
